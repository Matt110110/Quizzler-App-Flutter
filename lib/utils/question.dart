class Question {
  final String question;
  final bool answer;

  // Question(String question, bool answer) {
  //   this.question = question;
  // }
  Question(this.question, this.answer); // Same as the top, only easier to type and better 

}